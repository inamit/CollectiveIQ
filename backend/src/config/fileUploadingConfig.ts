import path from 'path';

const config = {
    uploadsAvatarsDirectory: path.join(__dirname, 'uploads/avatars'),
    uploadsPostsDirectory: path.join(__dirname, 'uploads/posts'),
};

export default config;